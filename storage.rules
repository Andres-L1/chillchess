rules_version = '2';

// Firebase Storage Security Rules for ChillChess
// The application currently does not use Firebase Storage.
// All paths are denied by default.

service firebase.storage {
  match /b/{bucket}/o {
    // Deny all access by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
